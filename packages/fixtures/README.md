# Fixtures

Reusable source and packaged fixtures for conformance and e2e tests live here.

Large generated WebC/Wasm artifacts should not be committed until we decide on
artifact storage and versioning. Source fixtures should stay small and readable.
